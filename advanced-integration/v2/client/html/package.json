{
    "name": "paypal-advanced-integration-frontend-html",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "GENERATE_SOURCEMAP=false vite build",
        "preview": "vite preview",
        "start": "GENERATE_SOURCEMAP=false vite",
        "format": "npx prettier --write **/*.{js,md}",
        "format:check": "npx prettier --check **/*.{js,md}"
    },
    "devDependencies": {
        "dotenv": "^16.4.5",
        "vite": "^5.4.2"
    }
}
